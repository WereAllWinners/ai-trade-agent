# Core ML/AI
# unsloth>=2026.5.0 — 5.x-compatible; 2026.2.x pins transformers<=4.57.6 (CVE-affected)
unsloth>=2026.5.0
torch>=2.0.0
# transformers>=5.0.0 — fixes CVE-2026-1839 (RCE in Trainer._load_rng_state)
transformers>=5.0.0
peft>=0.4.0
bitsandbytes>=0.41.0
accelerate>=0.20.0
trl>=0.7.0
datasets>=2.14.0

# Trading APIs
alpaca-py>=0.8.0
yfinance>=0.2.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
lxml==6.0.2
tenacity==9.1.4

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
pytz>=2023.3
# aiohttp>=3.13.5 — fixes 10 CVEs (DoS, header injection); pulled transitively by alpaca-py
aiohttp>=3.13.5

# Optional (for development)
jupyter>=1.0.0
matplotlib>=3.7.0

# Testing
pytest>=7.0.0